describe('Create Card Use Case', () => {
  test.todo('should validate that title, content and list are filled')

  describe('Create card repository', () => {
    test.todo('should call repository method correctly')

    test.todo('should throw an error if repository throw a low-level error')
  })

  test.todo('should return the created card if operation dont have errors')
})
